// this is where we define al our controllers

import '../controllers/menu_controller.dart';

class AppControllers {
  // the menu controller
  static AppMenuController menuController = AppMenuController.instance;
}
